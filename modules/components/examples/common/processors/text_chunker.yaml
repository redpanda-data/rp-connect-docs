processors:
  label: ""
  text_chunker:
    strategy: "" # No default (required)
    chunk_size: 512
    chunk_overlap: 100
    separators:
      - |+
        
      
      - |+
        
      - " "
      - ""
    length_measure: runes
    include_code_blocks: false
    keep_reference_links: false