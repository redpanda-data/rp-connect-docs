= git
// tag::single-source[]
:type: input
:categories: ["Services"]

component_type_dropdown::[]

Clones a Git repository, reads its contents, then polls for new commits at a configurable interval. Any updates are emitted as new messages.

ifndef::env-cloud[]
Introduced in version 4.51.0.
endif::[]

```yml
include::components:example$common/inputs/git.yaml[]
```

include::connect:components:partial$metadata/inputs/git.adoc[]