= oracledb_cdc
// tag::single-source[]
:type: input
:status: experimental
:categories: [Services]
:page-commercial-names: Oracle CDC, OracleDB CDC, Oracle Database CDC
:description: Enables Change Data Capture by consuming from OracleDB.

component_type_dropdown::[]

Enables Change Data Capture by consuming from OracleDB.

ifndef::env-cloud[]
Introduced in version 4.83.0.
endif::[]

Streams changes from an Oracle database for Change Data Capture (CDC).
Additionally, if `stream_snapshot` is set to true, existing data in the database is also streamed.

[tabs]
======
Common::
+
--

```yml
include::components:example$common/inputs/oracledb_cdc.yaml[]
```

--
Advanced::
+
--

```yml
include::components:example$advanced/inputs/oracledb_cdc.yaml[]
```

--
======

include::connect:components:partial$metadata/inputs/oracledb_cdc.adoc[]

== Permissions

When using the default Oracle-based cache, the Connect user requires permission to create tables and stored procedures, and the rpcn schema must already exist. See `checkpoint_cache_table_name` for more information.

include::connect:components:partial$fields/inputs/oracledb_cdc.adoc[]



// end::single-source[]