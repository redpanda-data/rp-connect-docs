= jira
// tag::single-source[]
:type: processor
:status: experimental
:categories: [Services]
:description: Queries Jira resources and returns structured data.

component_type_dropdown::[]

Queries Jira resources and returns structured data.

ifndef::env-cloud[]
Introduced in version 4.68.0.
endif::[]

[tabs]
======
Common::
+
--
[,yaml]
----
include::components:example$common/processors/jira.yaml[]
----
--
Advanced::
+
--
[,yaml]
----
include::components:example$advanced/processors/jira.yaml[]
----
--
======

Executes Jira API queries based on input messages and returns structured results. The processor handles pagination, retries, and field expansion automatically.

Supports querying the following Jira resources:

- Issues (JQL queries)
- Issue transitions
- Users
- Roles
- Project versions
- Project categories
- Project types
- Projects

The processor authenticates using basic authentication with username and API token. Input messages should contain valid Jira queries in JSON format.

include::redpanda-connect:components:partial$fields/processors/jira.adoc[]

include::redpanda-connect:components:partial$examples/processors/jira.adoc[]

// end::single-source[]