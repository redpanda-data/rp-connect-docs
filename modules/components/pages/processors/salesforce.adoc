= salesforce
// tag::single-source[]
:type: processor
:status: experimental
:categories: [Services]
:page-commercial-names: Salesforce
:description: Fetches data from Salesforce based on input messages.

component_type_dropdown::[]

Fetches data from Salesforce based on input messages.

ifndef::env-cloud[]
Introduced in version 4.85.0.
endif::[]

This processor takes input messages containing Salesforce queries and returns Salesforce data.

Supports the following Salesforce resources:

- sObject REST snapshot (all queryable sObjects or a filtered subset)
- Filtered SOQL queries via REST or GraphQL
- Change Data Capture (CDC) streaming via the Pub/Sub gRPC API
- Platform Events via the Pub/Sub gRPC API

[tabs]
======
Common::
+
--

```yml
include::components:example$common/processors/salesforce.yaml[]
```

--
Advanced::
+
--

```yml
include::components:example$advanced/processors/salesforce.yaml[]
```

--
======

include::redpanda-connect:components:partial$fields/processors/salesforce.adoc[]



// end::single-source[]