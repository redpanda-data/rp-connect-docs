|===
| Driver | Placeholder Style

| `clickhouse`
| Dollar sign (`$`)

| `mysql`
| Question mark (`?`)

| `postgres`
| Dollar sign (`$`)

| `mssql`
| Question mark (`?`)

| `sqlite`
| Question mark (`?`)

| `oracle`
| Colon (`:`)

| `snowflake`
| Question mark (`?`)

| `trino`
| Question mark (`?`)

| `gocosmos`
| Colon (`:`)
|===