|===
| Driver | Placeholder Style

| `clickhouse`
| Dollar sign (`$`)

| `gocosmos`
| Colon (`:`)

| `mysql`
| Question mark (`?`)

| `mssql`
| Question mark (`?`)

| `oracle`
| Colon (`:`)

| `postgres`
| Dollar sign (`$`)

| `snowflake`
| Question mark (`?`)

| `spanner`
| Question mark (`?`)

| `sqlite`
| Question mark (`?`)

| `trino`
| Question mark (`?`)
|===